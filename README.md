# Predicting Body Measurements

* See `artifacts/results/test_predicted.csv` for the predictions on `test.csv`. 
* See `instructions_and_questions.docx` for the written answers. 

Other:
* See `Predicting Measurements.ipynb` (or corresponding html files) for main notebook.
* See `artifacts/data_viz`, `artifacts/analysis` for figures created during data exploration and model analyses. 
* To run notebook, import conda environment from `environment.yml`, activate, and run notebook. 

